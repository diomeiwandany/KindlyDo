
export default function DeleteButton(props) {
    // console.log(props.task.id, "<<< From Delete Button");
    const handleDelete = async (id) => {

    }

    return (
        <>
            <button className="col btn btn-danger" onClick={() => {

            }}>
                Delete
            </button>
        </>
    )
}